(ns pandoras-dropbox.test.client-config-test
  (:require [clojure.test :refer :all]
            [pandoras-dropbox.client-config :refer :all]))



;; (deftest round-trip-config-test
;;   (testing "Round trip test for json config file"
;;     (let [start (hashmap :jid 0)
;;           config-file ]
;;       (spit


(deftest weeeee
  (testing "stuff"
    (do (println (System/getProperty "user.dir"))
        (is false))))
           
